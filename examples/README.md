# Examples

Versioned BPMN examples will be added as the corresponding backend capabilities become stable.

The first complete example should demonstrate a tenant-scoped approval flow with conditional assignment, process tracking, rejection, transfer, and an explicit audit trail. Agent examples must wait until the durable Agent Runtime exists; they must not rely on mocked capabilities presented as production behavior.
